_site
.bundle
.sass-cache
.jekyll-cache
.jekyll-metadata
.DS_store
.ruby-version
.tweet-cache
Gemfile.lock
assets/libs/
node_modules/
vendor
.idea

# Patent paperwork: internal legal/IP documents. This repo is public, so these
# stay on disk only. Delete these lines if you decide they should be published.
P3683US00*.pdf
Patent_*.pdf

# Claude Code local settings — machine-specific, not part of the site.
.claude/
